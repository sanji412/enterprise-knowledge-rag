# Baseline Verification — 2026-08-28

- Upstream commit: `8588513`
- Feature branch before product changes: `feature/chinese-enterprise-rag`
- Python: `3.13.15`
- Node.js: `24.15.0`
- npm: `11.12.1`

## Environment

The sandbox could not write to the default user-level uv and npm caches. The project therefore uses ignored local directories:

- `.uv-cache/`
- `.uv-python/`
- `.npm-cache/`
- `.venv/`

No user-level cache ownership or system Python installation was changed.

## Initial Upstream Results

- Python tests: `161 passed, 1 failed, 1 skipped`
- Failure: `test_both_scope_degrades_to_global_without_uploads`
- Root cause: the test double did not expose the recently added `embedding_profile` response field.
- Ruff: `12` existing issues under `evals/` (`7` automatically fixable, `5` line-length issues).

The outdated test double and Ruff-only issues were corrected without changing product behavior.

## Clean Baseline Results

- Python tests: `162 passed, 1 skipped`
- Python warnings: `126` upstream deprecation warnings
- Ruff: `All checks passed`
- Frontend: `5` test files passed, `9` tests passed
- TypeScript: passed
- Vite production build: passed
- npm audit: `8` upstream dependency advisories (`1 low, 1 moderate, 6 high`)

`npm audit fix` was intentionally not run because it may perform dependency upgrades outside baseline verification.

No product behavior was changed during baseline verification.
